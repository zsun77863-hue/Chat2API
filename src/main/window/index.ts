export { 
  createWindow, 
  getMainWindow, 
  showWindow, 
  hideWindow, 
  minimizeWindow, 
  maximizeWindow, 
  closeWindow,
  isWindowVisible,
  isWindowMaximized,
  isWindowMinimized,
  loadUrl,
  loadFile,
  reloadWindow,
  openDevTools,
  closeDevTools,
  toggleDevTools,
} from './manager'
export type { WindowOptions } from './manager'
